# Copy-paste-ready Codex handoff after ChatGPT Pro Output-12

Treat this handoff and the accompanying Output-12 audit as claims to reproduce. Modify only `AntonioAPDL/RQR-GIBBS`. Do not modify exdqlm or the Q-DESN article repository.

## Reviewed exact state

```text
corrected implementation:
  53dc71d873ef12ebba91cbc3d6813682e0987960

compact evidence:
  85658e9378d25b12335bf70e7de936b889ef74dd

package:
  rqrgibbs 0.1.0.9011

fit schema:
  rqrgibbs_fit/1.8.0

continuation schema:
  rqrgibbs_continuation_history/4.1.0

runtime attestation:
  rqrgibbs_runtime_attestation/5.0.0

bounded fixture schema:
  rqrgibbs_dlm_bounded_fixtures/5.0.0

bounded run schema:
  rqrgibbs_dlm_bounded_run/3.0.0

reference bundle:
  rqrgibbs_reference_bundle/2.0.0

estimand schema:
  rqrgibbs_dlm_bounded_estimands/1.0.0

wrapper closeout:
  rqrgibbs_dlm_wrapper_closeout/2.1.0
```

Protected read-only references:

```text
exdqlm:
  dffb71ee70b597d6a716ee74be1cbc99731cd453

Q-DESN article:
  f9f22804eff3871bb5350c8add04b7c9f4d4957b
```

## Output-12 decision

```text
statistical target and interpretation:             PASS
shell publication/finalization boundary:            PASS
manifest/actual regular-file equality:              PASS
8-scenario monitor/finalization suite:              PASS
RDS post-rename rollback:                           PASS
supported RNG-kind state lengths:                   PASS
runtime/reference/toolchain binding:                PASS
43 reference gates:                                 PASS as exact-commit evidence
6,000-retained benchmark:                           PASS, 150/150
remaining source launch blocker:                    NONE FOUND
create a separate authorization commit:             CONDITIONAL GO
run from the current false-flag commit:              NO
run after exact authorization rebuild and user gate: CONDITIONAL GO
matched/production simulation:                      NO-GO
CAVI/ELBO:                                           DEFER
RQR-DESN:                                            DEFER
```

The review independently rehashed the complete six-file Output-11 reconciliation packet. The exact outer manifest matched every byte count and SHA-256. It inspected all 43 reference rows and all 150 benchmark diagnostic rows. It did not rerun R/C++, package, TeX, runtime, or MCMC commands because the review environment had no R installation; those values remain exact-commit tracked evidence and must be regenerated at the authorization commit.

## Fixed scientific interpretation

Preserve these statements:

```text
RQR is a generalized-Bayes interval-root loss update.
The pseudo-AL construction augments the pseudo-residual loss kernel.
It is not an ordinary response likelihood.
Root/state draws are not posterior-predictive response draws.
Fixed W, frozen template, and shared component-scale modes use exact blocked
root-specific FFBS full-conditionals for their declared fixed-joint targets.
Adaptive conditional discount remains a working method and is excluded.
Same-data learned lambda is not automatic coverage calibration or response variance.
The bounded run does not establish empirical calibration or forecasting superiority.
```

## Next stage A: create one separate authorization commit

Do not alter the reviewed implementation while integrating this review. After independently rehashing the four Output-12 deliverables, create a new source commit whose only substantive configuration change is:

```text
application/config/rqr_dlm/rqr_dlm_bounded_dynamic_fixtures_20260723.R

bounded_dynamic_execution_authorized = FALSE
                                   -> TRUE
```

A version/documentation adjustment may record the authorization state if repository policy requires it, but do not change any scientific or computational control. In particular, do not change:

```text
fixtures
learning-rate modes or target formulas
coverage level
seeds
initialization profiles
4 chains
2,000 burn-in
6,000 retained draws
thin 1
backend cpp
numerical policy fail
R-hat <= 1.01
bulk ESS >= 1,000
tail ESS >= 1,000
zero-repair rule
240-minute ceiling
process/thread/RSS ceilings
artifact schemas
cell-level stop rules
```

Commit and push the authorization source separately. Do not run the 24 fits in the same unreviewed source state.

## Next stage B: rebuild all exact authorization evidence

At the full authorization SHA:

1. require clean `main` at that exact SHA;
2. build `rqrgibbs` from an exact Git archive into a new disjoint isolated runtime;
3. verify every version-5 lineage gate and preserve the attestation hash;
4. run exact preflight and require 24 prospective fits with zero executed fits;
5. run the complete reference-only suite under the same runtime/toolchain;
6. require 43/43 gates, zero failures, final PGID empty, and a complete recursive artifact manifest;
7. retain the unchanged monitor fault suite and require 8/8;
8. record the new config digest, runtime tree, attestation, toolchain, reference-gate hash, reference-bundle hash, and artifact-manifest hash; and
9. run the fail-closed execute check without user confirmation to prove the new gate still requires the phrase.

Because the authorization changes the source/config digest, do not reuse the Output-11 runtime, preflight manifest, or reference bundle as execution authority. The unchanged full reference/diagnostic tables may be used only as comparison targets, not as the new binding bundle.

## Next stage C: stop for explicit user confirmation

After the authorization commit and all fresh exact-source gates pass, stop and report the full authorization SHA plus the new runtime/reference binding. Obtain the user's explicit confirmation phrase:

```text
I_CONFIRM_24_BOUNDED_RQR_DLM_FITS
```

Do not infer consent from earlier messages, the Output-12 conditional go, or a request to continue generally. Do not place the phrase on the user's behalf.

## Next stage D: execute only after confirmation

Once the exact confirmation is supplied, execute the six four-chain cells sequentially through the reviewed wrapper. After each cell:

```text
validate the independent exact estimand schema
evaluate all maintained diagnostics
require exact target and exact runtime provenance
require zero numerical and forecast repairs
require continuation/checkpoint integrity
require final PGID emptiness and resource pass
require an empty structured failure log
publish chains only through readback-verified RDS rollback guards
publish compact evidence and recursive hashes
stop before later cells on any failure
```

No retries, extensions, retuning, threshold changes, seed substitution, or selective reruns are allowed. Full chain files remain ignored and local; compact tables and hashes may be tracked according to the established policy.

## Next stage E: audit before any broader work

After the 24-fit run, produce a separate bounded-grid reconciliation and another independent audit. Even a complete pass establishes only target mechanics, numerical execution, provenance, continuation, artifact integrity, and bounded mixing.

Do not begin any of the following from the authorization or bounded-run result alone:

```text
matched or production RQR-DLM simulation
coverage-calibration claims
forecast-superiority claims
response-predictive scoring
CAVI/ELBO implementation
RQR-DESN work
```

## Nonblocking residuals to preserve honestly

- PGID RSS is sampled telemetry, not a kernel-hard peak.
- Cleanup cannot be guaranteed after SIGKILL or a filesystem refusal to unlink.
- The bounded artifact names are controlled; the newline-based verifier is not a general arbitrary-filename manifest.
- The RDS helper's dangling-symlink case is optional hardening and is unreachable in the fresh bounded output directory.
- Remote CI remains optional; it is not a substitute for the exact local runtime chain.

## Required Codex closeout

Return:

1. the exact authorization commit SHA;
2. the exact config diff;
3. the new isolated-runtime and attestation identities;
4. preflight/reference/monitor/fail-closed results and hashes;
5. confirmation that zero bounded-grid fits ran before explicit user approval;
6. the exact phrase the user must supply; and
7. a copy-paste-ready prompt for the post-authorization reviewer or execution gate.

Do not modify protected repositories. Do not commit heavy chains, fitted objects, runtime caches, compiler outputs, TeX build products, or run directories.
