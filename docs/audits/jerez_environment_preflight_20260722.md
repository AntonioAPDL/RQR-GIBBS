# Jerez Environment Preflight

Date: 2026-07-22

## Purpose

This audit records the initial Jerez bootstrap for the standalone RQR-GIBBS
project. It confirms that the new manuscript repo, the pinned exdqlm RQR
implementation branch, and the Q-DESN article style reference are all available
under the intended Jerez workspace.

## Workspace

```text
server: jerez.be.ucsc.edu
base path: /data/muscat_data/jaguir26/local/src
```

## Repositories

| Repo | Branch | Commit | Dirty count | Role |
|---|---|---:|---:|---|
| `RQR-GIBBS` | `main` | `4866b5572b600b88ca389be41e5830c4ad1326c7` | 0 | Standalone article and reproducibility repo |
| `exdqlm__wt__qdesn_0p4p0_integration` | `feature/rqr-desn-readout-20260716` | `dffb71ee70b597d6a716ee74be1cbc99731cd453` | 0 | Pinned RQR implementation source of truth |
| `Article-Q-DESN---Version-2` | `main` | `d9937a3f4e99156e0f6fa7cae17a297d906e85f0` | 0 | Style/reference repo only |

## Tool Preflight

`make smoke` passed on Jerez.

Observed environment:

```text
R: 4.5.3
git: available
pdflatex: available
bibtex: available
latexmk: missing from PATH
testthat: installed
pkgload: installed
jsonlite: installed
yaml: installed
digest: installed
```

The `Makefile` fallback to `pdflatex` plus `bibtex` is therefore required on
Jerez unless `latexmk` is installed later.

## Build Validation

Both manuscript files compiled on Jerez:

```text
make pdf
make supplement
```

Final log scan:

```text
rg -n "undefined|Warning|Error|Emergency" main.log rqr-gibbs-supplement.log
```

Result: no matches in the final logs.

## RQR Implementation Validation

The focused package-side RQR smoke tests passed on Jerez through:

```text
make test-exdqlm-rqr
```

The test target loaded the pinned exdqlm branch and ran:

```text
test-rqr-algebra.R
test-rqr-mcmc-fixed-design.R
test-rqr-learned-scale-mcmc.R
test-rqr-desn-design-parity.R
test-rqr-forecast-contract.R
```

All reported `DONE`.

## Literature PDFs

The local-only literature folder was populated on Jerez:

```text
literature/pdfs/
```

The checksum manifest was generated by:

```text
make literature-manifest
```

Result:

```text
18 PDFs
literature/pdf_manifest.csv
literature/SHA256SUMS
```

These files are intentionally ignored by git.

## Current Decision

Jerez is ready for a new Codex chat to inspect and continue the standalone
RQR-GIBBS project. Heavy simulations should still wait until the next chat
confirms the scaffold, implements or validates the RQR-DLM/FFBS path, and
freezes a matched standalone simulation manifest.

