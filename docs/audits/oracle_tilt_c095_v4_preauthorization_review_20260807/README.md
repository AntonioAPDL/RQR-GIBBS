# Oracle-tilt V4 preauthorization review packet

This compact packet supports an independent decision on whether the version-4
prospective illustration screen may advance to a flag-only authorization
commit. The authoritative scientific source is the packet's parent commit:

```text
4b38e6a556e6848c7ed6e077dad0d22a00367382
```

The packet contains exact closeouts, gate tables, runtime/source bindings,
resource telemetry, complete manifest inventories, and compact summaries from
the four preauthorization stages. It intentionally excludes approximately
15 GiB of synthetic endpoint arrays from the resource rehearsal and all fitted
MCMC objects. The complete wrapper manifest records the omitted files' paths,
byte counts, and SHA-256 values.

The review-branch commit adds evidence and review instructions only. It is not
an authorization commit and must never be used as the source identity for the
production run.

Read `independent_review_request.md` first. Treat
`preauthorization_reconciliation.md` and all closeouts as claims to audit, not
as proof by assertion.

