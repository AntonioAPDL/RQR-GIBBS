# RQR Implementation Seed Files

The R files in this directory are synchronized seed copies from:

```text
/data/jaguir26/local/src/exdqlm__wt__qdesn_0p4p0_integration
branch: feature/rqr-desn-readout-20260716
commit: dffb71ee70b597d6a716ee74be1cbc99731cd453
```

They are included so the standalone RQR-GIBBS repository has an auditable code
starting point. The validated executable implementation remains the exdqlm
branch until these files are promoted into a native standalone package or
script API.

