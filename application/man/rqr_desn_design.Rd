% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rqr_desn_design.R
\name{rqr_desn_design}
\alias{rqr_desn_design}
\title{Construct a frozen RQR-DESN feature-design contract}
\usage{
rqr_desn_design(
  X,
  y,
  time_index = seq_len(nrow(as.matrix(X))),
  feature_names = colnames(as.matrix(X)),
  intercept = NULL,
  builder,
  reservoir,
  driver = list(type = "observed_history", response_simulation = FALSE),
  causal = list(uses_current_response = FALSE, uses_future_response = FALSE),
  time = list(),
  terminal = list(available = FALSE)
)
}
\arguments{
\item{X}{Finite numeric feature matrix.}

\item{y}{Numeric response vector aligned with the rows of \code{X}. \code{NA} entries
are allowed, but at least one response must be observed.}

\item{time_index}{Strictly increasing numeric index aligned with \code{X}.}

\item{feature_names}{Unique ordered feature names.}

\item{intercept}{Optional verified constant-one intercept declaration.}

\item{builder}{Plain metadata identifying the feature builder and version.}

\item{reservoir}{Plain reservoir metadata with a SHA-256 \code{digest}.}

\item{driver}{Plain training-history driver metadata.}

\item{causal}{Plain causal-contract metadata.}

\item{time}{Additional plain time metadata.}

\item{terminal}{Optional terminal-state and lag-buffer digest metadata.}
}
\value{
A versioned \code{rqr_desn_design} object.
}
\description{
This constructor records an already materialized deterministic DESN feature
design. It does not generate reservoir states and does not authorize
response simulation.
}
