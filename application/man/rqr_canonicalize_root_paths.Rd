% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rqr_root_labels.R
\name{rqr_canonicalize_root_paths}
\alias{rqr_canonicalize_root_paths}
\title{Canonicalize exchangeable RQR-DLM root paths}
\usage{
rqr_canonicalize_root_paths(
  eta_root1,
  eta_root2,
  reference_eta_lower = NULL,
  reference_eta_upper = NULL,
  time_weights = NULL,
  gap_tolerance = 1e-08,
  ambiguity_tolerance = 1e-08,
  reference_method = if (is.null(reference_eta_lower) && is.null(reference_eta_upper)) {

        "iterative_consensus"
 } else {
     "provided"
 },
  max_iter = 20L,
  fail_on_ambiguous = FALSE
)
}
\arguments{
\item{eta_root1, eta_root2}{Time-by-draw raw root ordinate matrices.}

\item{reference_eta_lower, reference_eta_upper}{Optional reference lower/upper
paths. If omitted, an iterative consensus reference is used.}

\item{time_weights}{Optional nonnegative weights over time points.}

\item{gap_tolerance}{Nonnegative tolerance for declaring root crossings or
nonseparated references.}

\item{ambiguity_tolerance}{Nonnegative tolerance for declaring keep/swap
assignment ties.}

\item{reference_method}{Either `"provided"` or `"iterative_consensus"`.}

\item{max_iter}{Maximum consensus iterations for `"iterative_consensus"`.}

\item{fail_on_ambiguous}{If `TRUE`, stop when the global chart is not
supported. If `FALSE`, return a diagnostic object with `status != "ok"`.}
}
\value{
A `rqr_root_path_label_diagnostics` object.
}
\description{
This helper applies the same complete-root-block principle to retained
dynamic root ordinate paths. The keep/swap decision is one binary assignment
per retained path, based on a weighted path-space distance to a reference
lower/upper path. It does not relabel individual times separately.
}
