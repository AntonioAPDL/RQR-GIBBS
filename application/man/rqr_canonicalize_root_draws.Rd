% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rqr_root_labels.R
\name{rqr_canonicalize_root_draws}
\alias{rqr_canonicalize_root_draws}
\title{Canonicalize exchangeable static RQR root draws}
\usage{
rqr_canonicalize_root_draws(
  beta_root1,
  beta_root2,
  X_audit,
  reference_beta_lower = NULL,
  reference_beta_upper = NULL,
  audit_weights = NULL,
  gap_tolerance = 1e-08,
  ambiguity_tolerance = 1e-08,
  reference_method = if (is.null(reference_beta_lower) && is.null(reference_beta_upper))
    {
     "iterative_consensus"
 } else {
     "provided"
 },
  max_iter = 20L,
  fail_on_ambiguous = FALSE
)
}
\arguments{
\item{beta_root1, beta_root2}{Retained raw root coefficient draws, as
draw-by-coefficient matrices.}

\item{X_audit}{Design matrix defining the audit domain on which a global
lower/upper coefficient labeling is requested.}

\item{reference_beta_lower, reference_beta_upper}{Optional reference
lower/upper coefficient vectors. If omitted, `reference_method` must be
`"iterative_consensus"`.}

\item{audit_weights}{Optional nonnegative row weights for `X_audit`.}

\item{gap_tolerance}{Nonnegative tolerance for declaring root crossings or
nonseparated references.}

\item{ambiguity_tolerance}{Nonnegative tolerance for declaring keep/swap
assignment ties.}

\item{reference_method}{Either `"provided"` or `"iterative_consensus"`.}

\item{max_iter}{Maximum consensus iterations for `"iterative_consensus"`.}

\item{fail_on_ambiguous}{If `TRUE`, stop when the global chart is not
supported. If `FALSE`, return a diagnostic object with `status != "ok"`.}
}
\value{
A `rqr_root_label_diagnostics` object. Canonical coefficient draws
  are populated only when `status == "ok"`.
}
\description{
RQR root coefficients are exchangeable under the symmetric two-root target.
This post-processing helper tries to assign each complete root block to a
single lower/upper coefficient chart on a declared audit design. It never
alters the Markov chain state, never relabels coefficients componentwise, and
fails closed when the fitted linear roots cross or are assignment-ambiguous
on the audit domain.
}
